# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env

# OS / editors
.DS_Store
.idea/
.vscode/

# Streamlit
.streamlit/secrets.toml

# Claude Code local settings
.claude/

# Local dev smoke-test helper (not part of the app)
appcheck.py

# Raw CAASPP source data (regenerable from CDE; preprocessed into school_scores/*.parquet)
school_scores/*.txt
school_scores/*.zip
school_scores/*.mdb
school_scores/*.png

# Full SEDA parquets (kept locally as *_full.parquet backups; deployed app uses Burlingame-trimmed versions)
seda_app/data/*_full.parquet

# Raw CDE source downloads (regenerable; preprocessed into burlingame_*.parquet/csv)
seda_app/data/staffing_raw/
seda_app/data/frpm_raw/
seda_app/data/absenteeism_raw/
seda_app/data/spending_raw/
seda_app/data/dashboard_raw/
